name: base
channels:
  - conda-forge
dependencies:
  - python=3.11
  - ipykernel # required for jupyter notebook in VS Code
  - pandas
  - numpy
  - seaborn
  - scikit-learn
  - python-dotenv
  - geopandas
  - requests
  - ipywidgets
  - langchain=0.3.0
  - langchain-google-genai=2.0.0
  - langchain-experimental=0.3.0
  # DVC removed - migrated to local parquet with metadata tracking
  # See docs/20250120-parquet-migration-design.md for details
  - tabulate=0.9.0
  - langgraph=0.2.32
  - langgraph-checkpoint==2.0.0
  - langchain-community
  - beautifulsoup4
#   - pip  # if you need more packages installed from pip
# DVC removed - migrated to local parquet with metadata tracking
# See docs/20250120-parquet-migration-design.md for details
  - pip:
    - h3==4.1.0b2
    - boto3
    - awscli
    - streamlit
    - pygwalker
    - supabase
