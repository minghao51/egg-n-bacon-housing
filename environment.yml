name: demo
channels:
  - conda-forge
dependencies:
  - python=3.11
  - ipykernel # required for jupyter notebook in VS Code
  - pandas
  - numpy
  - seaborn
  - scikit-learn
  - python-dotenv
  - geopandas
  - requests
#   - pip  # if you need more packages installed from pip 
  - pip:
    - 